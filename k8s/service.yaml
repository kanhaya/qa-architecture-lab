apiVersion: v1
kind: Service

metadata:
  name: loan-service
  namespace: qa-lab

spec:
  type: NodePort

  selector:
    app: loan-service

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
